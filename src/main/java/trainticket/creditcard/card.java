package trainticket.creditcard;

public class card {

	/*
	 * public static PaymentResponse cardpayment(long cardno, LocalDate exp, int
	 * cvv, float amount, String comments) { creditcard.CreditCard creditCard = new
	 * creditcard.CreditCard(); creditCard.setCardNo(cardno);
	 * creditCard.setExpiryDate(exp); creditCard.setCvvNo(cvv); PaymentResponse obj
	 * = null; try { String id = "Reserveme"; obj =
	 * CreditCardService.pay(creditCard, amount, id, comments); int transId
	 * =obj.getTransactionId(); if(obj.isStatus()) {
	 * 
	 * }
	 * 
	 * } catch (Exception e) { e.printStackTrace();
	 * 
	 * } return obj;
	 * 
	 * }
	 */

}
